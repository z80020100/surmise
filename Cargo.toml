[package]
name = "surmise"
version = "0.1.0"
edition = "2024"
# The pinned toolchain, which is the only one CI builds. Edition 2024 needs
# 1.85 at the least, so this can drop once a job proves the lower bound.
rust-version = "1.98.0"
description = "TBD"
license = "MIT OR Apache-2.0"
repository = "https://github.com/z80020100/surmise"
# Nothing here is ready to ship. Before removing this, add an `exclude` list,
# because `cargo package` collects every file git does not ignore.
publish = false

# TBD. The template's dependencies are gone. Nothing has replaced them yet.
[dependencies]

[dev-dependencies]
cargo-husky = { version = "1.5.0", default-features = false, features = ["user-hooks"] }
