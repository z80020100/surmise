[package]
name = "surmise"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
description = "TBD"
license = "MIT OR Apache-2.0"
repository = "https://github.com/z80020100/surmise"

# TBD. The template's dependencies are gone. Nothing has replaced them yet.
[dependencies]

[dev-dependencies]
cargo-husky = { version = "1.5.0", default-features = false, features = ["user-hooks"] }
