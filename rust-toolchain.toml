# Pinned on purpose. `.cargo/config.toml` sets -Dwarnings, so a new lint in a
# new stable would turn CI red with no change to the code. Bump this file to
# take a new toolchain and answer the lints it brings in the same commit.
[toolchain]
channel = "1.98.0"
components = ["clippy", "rustfmt"]
